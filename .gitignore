# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# orch runtime state — keep the config, ignore the rest
.orch/*
!.orch/orch.yml

# session/local scratch
.remember/

# Claude Code local/per-user settings — .claude/settings.json is shared
.claude/settings.local.json

# ESPHome local secrets (per-user printer entity, etc.)
esphome/secrets.yaml
